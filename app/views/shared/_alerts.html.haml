- if flash[:alert]
  = js add_gritter(flash[:alert], image: :alert, sticky: false)
- elsif flash[:error]
  = js add_gritter(flash[:error], image: :error, sticky: false)
- else
  = js add_gritter(flash[:notice], image: :notice, sticky: false)